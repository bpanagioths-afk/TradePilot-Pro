import DashboardPlaceholderWidget from "../DashboardPlaceholderWidget";

export default function RiskWidget() {
    return (
        <DashboardPlaceholderWidget
            title="Risk Widget"
            description="Risk limits, exposure and rule alerts will appear here."
        />
    );
}